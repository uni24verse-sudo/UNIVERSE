{
  "name": "universe-monorepo",
  "version": "1.0.0",
  "description": "Root package for automatic deployment",
  "scripts": {
    "build": "cd backend && npm install",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
